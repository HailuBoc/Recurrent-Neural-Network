# Simple Vanilla RNN in Python

This project is a **simple Recurrent Neural Network (RNN)** implemented from scratch using **Python and NumPy**.  
It demonstrates a basic sequence prediction task where the network learns to predict the next number in a small sequence.

## Project Structure

